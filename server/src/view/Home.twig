{% extends "partial/MainLayout.twig" %}

{% block title %}
    Home
{% endblock %}

{% block section %}
    <h1>Home</h1>

    {% if user %}
        <a href="{{ urlFor('auth.logout') }}">Logout</a><br>
        <h2>
            Xin chào
            {{ user.userName }}!
        </h2>
    {% else %}
        <a href="{{ urlFor('auth.login') }}">Login</a><br>
        <a href="{{ urlFor('auth.register') }}">Register</a>
    {% endif %}
{% endblock %}
